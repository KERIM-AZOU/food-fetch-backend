# Backend Environment Variables
# Copy this file to .env and fill in your API keys

# Server Configuration
FETCH_PORT=3000

# HMAC Request Signing - shared secret for request signatures
HMAC_SECRET=your_hmac_secret_here

# Request timestamp tolerance in seconds (prevents replay attacks)
HMAC_TOLERANCE=300

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:5174,http://localhost:5173

# Rate limiting (requests per minute per IP)
RATE_LIMIT_MAX=100

# Groq API - Used for transcription (Whisper), chat (Llama), TTS (PlayAI), extraction, and translation
# Get your key at https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# OpenAI API (optional fallback - commented out in code)
# Get your key at https://platform.openai.com
# OPENAI_API_KEY=your_openai_api_key_here
